# Parker 0.1.32 Release Notes

Status: Draft

`0.1.32` starts with Advanced Search fixes, field-aware rule operators, and a mobile comic-detail layout adjustment.

## Added

- Added foundational reader overlay infrastructure with a debug mode for validating page-aligned SVG overlays, normalized page coordinates, and future annotation support.
- Added persisted reader annotations for page pins and rectangle regions, including user-owned storage, API endpoints, reader loading, SVG overlay rendering, drag repositioning, and edit/delete controls.


## Changed

- Advanced Search now offers rule operators based on the selected field, hiding impossible options such as empty library or series names and using numeric comparisons for release years.
- Advanced Search now normalizes loaded or deep-linked rules whose saved operator no longer applies to the selected field.
- Comic detail pages now move File Info below the main issue details on narrow mobile screens.
- Improved paged Fit Width reader behavior so oversized pages can scroll vertically while preserving edge navigation and cursor affordance.
- Reader annotation mode now pauses page navigation gestures while placement/editing tools are active and keeps annotation changes read-only during incognito sessions.
- Reader annotation pins now render as map-style markers with the pointed tip anchored to the saved page coordinate.

## Fixed

- Fixed Advanced Search creator `Not Equals` filters, including searches like `Penciller != Alex Ross`, so matching creator credits are excluded instead of the rule being ignored when combined with other filters such as `Language = en`.
- Fixed additional Advanced Search field/operator mismatches so genre empty checks, library name filters, and collection, reading-list, and stack negative/all-match filters build real backend conditions.
- Fixed annotation panel text entry so spaces and other reader hotkeys do not trigger page navigation while editing annotation titles or notes.

## Validation

- Verified focused search service coverage after the creator exclusion and UI-aware operator-matrix hardening: `217 passed` via `.\.venv\Scripts\python.exe -m pytest tests\services\test_search_service.py`.
- Verified Advanced Search field-aware operator menus in the browser: `1 passed` via `.\.venv\Scripts\python.exe -m pytest tests\browser\test_search_and_series_smoke.py::test_search_operator_options_match_selected_field`.
- Verified mobile comic-detail File Info ordering on narrow screens: `1 passed` via `.\.venv\Scripts\python.exe -m pytest tests\browser\test_saved_search_pull_list_and_session_flows.py::test_comic_detail_file_info_moves_to_bottom_on_mobile`.
- Verified reader overlay alignment in single-page, double-page, Fit Screen, Fit Height, Fit Width, and Long View modes.
- Verified Fit Width scrolling, edge navigation, cursor behavior, and normalized coordinate reporting.
- Verified annotation API, bookmark API, and reader init/page contracts: `25 passed` via `.\.venv\Scripts\python.exe -m pytest tests\api\test_annotations.py tests\api\test_bookmarks.py tests\api\test_reader_overlay_shell.py tests\api\test_reader.py`.
- Verified reader annotations create/save/reload/render/reposition/edit/delete in the browser as part of the full reader smoke suite: `19 passed` via `.\.venv\Scripts\python.exe -m pytest tests\browser\test_reader_smoke.py`.
