# Parker 0.1.37 Release Notes

Status: Draft

`0.1.37` improves admin job visibility so long-running scan pipelines are easier to monitor while also expanding thumbnail job diagnostics for warning-only image issues, including malformed EXIF metadata reported by Pillow.

## Added

- Added a live Active Queue section to admin Job History showing running and queued background jobs, worker-order queue positions, job type, library, timestamps, and elapsed running or queued time.
- Added thumbnail job warning summaries and per-comic warning details to the admin Job History details modal, including comic ID, file path, warning type, and warning message.

## Changed

- Thumbnail workers now capture Python image-processing warnings during cover generation and save them into the job summary instead of leaving them as unattributed process log output.
- Warning-only thumbnail results remain successful thumbnail generations while still surfacing their diagnostic details separately from hard errors.
- Bumped the archive page-order cache version so readers refresh cached page ordering after the latest cover detection rule change.

## Fixed

- Fixed thumbnail scan diagnostics for malformed image metadata warnings so admins can identify the affected comic file instead of seeing only a raw Pillow warning in server logs.
- Fixed tagged-version checks so multi-worker deployments share the version-check cache through a locked cache file instead of each worker independently querying GitHub after cache warm-up.
- Fixed version-check cache lock fallback handling so `portalocker` lock failures return an uncached version status instead of surfacing as endpoint errors.
- Fixed archive cover selection when a zero-letter page variant shares the same trailing title as a bare page-zero cover, such as `TarzanJohnCarter1-00 Red Awakenings.jpg` before `TarzanJohnCarter1-00a Red Awakenings.jpg`.

## Validation

- Verified active queue API ordering, admin page rendering, and the browser smoke path for running plus queued jobs: `63 passed` via `.\.venv\Scripts\python.exe -m pytest tests/api/test_jobs.py tests/api/test_pages.py tests/browser/test_admin_jobs_queue.py -q`.
- Verified active queue page rendering and browser behavior after the background polling repaint mitigation and adaptive queue polling: `57 passed` via `.\.venv\Scripts\python.exe -m pytest tests/api/test_pages.py tests/browser/test_admin_jobs_queue.py -q`.
- Verified thumbnail warning capture, thumbnail writer summaries, and job summary API behavior: `25 passed` via `.\.venv\Scripts\python.exe -m pytest tests\services\test_thumbnailer_service.py tests\api\test_jobs.py`.
- Verified version-check service behavior, including true spawned-process shared-cache coordination and lock-failure fallback coverage: `12 passed` via `.\.venv\Scripts\python.exe -m pytest tests\services\test_version_check.py -q`.
- Verified archive cover regression coverage and reader API cache-key behavior after the zero-letter page variant fix: `35 passed` via `.\.venv\Scripts\python.exe -m pytest tests\services\test_archive.py tests\api\test_reader.py -q`.
- The sandboxed Python validation processes failed to start with the known `encodings` import error, so validation used the repository-prescribed elevated retry.
